openapi: "3.0.0"

info:
  title: "Stub API"
  version: "0.2.0"

servers:
  - url: "/api/v1"

paths:
  "/info":
    get:
      operationId: "thoth.stub.api.info_get"
      tags:
        - "info"
      summary: "Get some information about this API."
      responses:
        "200":
          description: "Successful response with information."
